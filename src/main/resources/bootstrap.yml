server:
  port: ${SERVER_PORT:8080} # Server HTTP port.
  servlet.context-path: / # Context path of the application.

info:
  service_name: ${spring.application.name}
  description: Instance of a reactive manager
  maven_version: ${project.version}